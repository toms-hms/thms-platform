---
name: Contractor detail page with job history
description: GET /contractors/:id route + detail page showing name, company, categories, zip codes, contact info, notes, and a job history table with per-job status badges; contractor cards now navigate to the detail page on click
type: project
---

Factory ran ticket **THM-11** on 2026-05-07.

**PR:** https://github.com/toms-hms/thms-platform/pull/15
**Cost:** $1.35

**Files changed:**
- apps/api/src/contractor/models/ContractorManager.ts — added `listJobHistory(contractorId)` JOIN across JobContractor → Job → Home
- apps/api/src/contractor/route.ts — added `GET /:contractorId/jobs` behind existing `permit` middleware
- apps/api/src/contractor/service.ts — added `getContractorJobHistory()` with contractor existence check
- apps/web/src/app/(dashboard)/contractors/[contractorId]/page.tsx — new detail page
- apps/web/src/app/(dashboard)/contractors/page.tsx — cards now navigate on click with `stopPropagation` on action buttons
- apps/web/src/app/(dashboard)/contractors/queries.ts — added `getContractorJobs(contractorId)`

**Key decisions:**
- Edit button only rendered when `user.role === 'ADMIN'`
- Job history fetched via a separate route rather than embedding in the contractor response
