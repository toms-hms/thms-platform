---
name: TS build error fixes — ai service, upload service, shared exports, factories, lru-cache, seed
description: Fixed six residual TypeScript errors blocking the apps/api build; key decisions were aliasing the duplicate my-vendor DTO export in the shared index and typing lru-cache locally inside PermissionService.
type: project
---

Factory ran ticket **THM-19** on 2026-05-13.

**PR:** (pending)
**Files changed:**
- `apps/api/src/ai/service.ts` — import `AiSession` from `@thms/shared`; rewrite `DiagnoseResult.summary` as `AiSession['summary']`
- `apps/api/src/upload/service.ts` — replace `JobManager.findByIdForUser` calls with `JobManager.hasPermission` + `NotFoundError`
- `packages/shared/src/index.ts` — alias `ContractorDto` from `my-vendor.ts` as `VendorContractorDto` to avoid the re-export collision with `contractor.ts`
- `apps/api/src/job/factories/JobContractor.factory.ts` — add all required `JobContractor` model fields (`id`, `status`, `lastContactedAt`, `lastResponseAt`, `notes`, `createdAt`, `updatedAt`)
- `apps/api/src/permissions/PermissionService.ts` — use a local constructor/type alias for `lru-cache` instead of a separate ambient declaration file
- `apps/api/src/db/seed.ts` — change contractor seed data from `category: TradeCategory.X` to `categories: [TradeCategory.X]`

**Key decisions:**
- `ContractorDto` exists in both `contractor.ts` and `my-vendor.ts` with different shapes; the shared index now re-exports the vendor version under `VendorContractorDto` so callers keep the contractor DTO and the user-contractor DTO separated without a source-file rename.
- Used a local constructor/type alias in `PermissionService.ts` instead of adding `@types/lru-cache` or an ambient `src/@types` file, keeping the fix inside the ticket scope.
