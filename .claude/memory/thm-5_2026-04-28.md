---
name: THM-5 run 2026-04-28
description: AiSession JSONB model on Job; summary union discriminated by `intent`; shared types: ChatMessage, IssueSummary, ImprovementSummary, RecurringSummary, AiSessionSummary, AiSession
type: project
---

Factory ran ticket **THM-5** on 2026-04-28.

**PR:** https://github.com/toms-hms/thms-platform/pull/8
**Files changed:**
- packages/shared/src/types/ai.ts (ChatMessage, IssueSummary, ImprovementSummary, RecurringSummary, AiSessionSummary, AiSession)
- packages/shared/src/types/job.ts (aiSession field added to JobDto)
- apps/api/src/job/models/Job.ts (ai_session column, jsonb)
- apps/api/src/job/schema.ts (AiSession zod validation)
- apps/api/src/job/factories/Job.factory.ts (aiSession default)

**Key decisions:**
- Discriminant field on summary union types is `intent` (matches DB column name), not `kind`
- AiSession stored as JSONB in Job table
- Summary union: IssueSummary | ImprovementSummary | RecurringSummary, discriminated by `intent`
