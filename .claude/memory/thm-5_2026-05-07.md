---
name: Job AI session schema
description: Job diagnosis storage was normalized to aiSession/ai_session with intent-discriminated summaries shared between API validation and package types.
type: project
---

Factory ran ticket **THM-5** on 2026-05-07.

**PR:** (pending)
**Files changed:**
- apps/api/src/db/migrations/0001_rename_diagnosis_to_ai_session.sql
- apps/api/src/job/schema.ts
- apps/api/src/job/service.ts
- apps/api/src/job/__tests__/route.test.ts
- packages/shared/src/types/job.ts

**Key decisions:**
- Kept `intent` as the summary union discriminant to match the existing shared `IssueSummary`, `ImprovementSummary`, and `RecurringSummary` types, and added a guarded SQL migration that renames legacy `diagnosis` when present while preserving existing `ai_session` databases.
