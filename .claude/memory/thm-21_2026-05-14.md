---
name: API pattern refactor — TypedRequest, homeJobRouter, service mutations only
description: Applied THM-20 patterns globally across all API modules: TypedRequest utility type, homeJobRouter rename, service=mutations only, GET reads inline via managers, @/ imports everywhere, schema-named request types, and test coverage for all modules.
type: project
---

Factory ran ticket **THM-21** on 2026-05-14.

**PR:** (pending)
**Files changed:**
- `apps/api/src/middleware/auth.middleware.ts` — added `TypedRequest<P,Q,B>` utility type
- `apps/api/src/job/route.ts` — complete rewrite: `homeJobRouter` (was `jobCollectionRouter`), `@/` imports, TypedRequest handlers, inline GET logic via managers, removed category-suggestions route
- `apps/api/src/job/schema.ts` — added `JobSchema`, `HomeJobsSchema`, `JobsSchema`, request types
- `apps/api/src/job/service.ts` — removed `getJob`, `listJobContractors`, `suggestTradeCategories`, `CATEGORY_RULES`, `CATEGORY_LABELS`; `@/` imports
- `apps/api/src/home/route.ts` — TypedRequest handlers, `@/` imports, inline GET with formatAddress
- `apps/api/src/home/schema.ts` — added `HomeSchema`, `HomeRequest`, `HomesRequest`
- `apps/api/src/home/service.ts` — removed `getHome`; `@/` imports
- `apps/api/src/auth/route.ts` — TypedRequest handlers, `@/` imports
- `apps/api/src/auth/schema.ts` — added `AuthRequest`
- `apps/api/src/contractor/route.ts` — TypedRequest handlers, `@/` imports, inline GET via manager
- `apps/api/src/contractor/schema.ts` — added `ContractorSchema`, `ContractorRequest`, `ContractorsRequest`
- `apps/api/src/contractor/service.ts` — removed exported `getContractor`, inlined into mutations
- `apps/api/src/quote/route.ts` — TypedRequest handlers, `@/` imports
- `apps/api/src/quote/schema.ts` — added `QuoteSchema`, `JobQuotesSchema`, request types
- `apps/api/src/quote/service.ts` — removed `listQuotes`
- `apps/api/src/communication/route.ts` — TypedRequest handlers, `@/` imports, inline GET via manager
- `apps/api/src/communication/schema.ts` — added `CommunicationSchema`, `JobCommunicationsSchema`, request types
- `apps/api/src/communication/service.ts` — removed `listCommunications`, `getCommunication`
- `apps/api/src/integration/route.ts` — TypedRequest handlers, `@/` imports, inline GET via manager
- `apps/api/src/integration/schema.ts` — new file: `IntegrationSchema`, `IntegrationRequest`, `IntegrationsRequest`
- `apps/api/src/integration/service.ts` — removed `listIntegrations`
- `apps/api/src/userContractor/route.ts` — TypedRequest handlers
- `apps/api/src/userContractor/schema.ts` — added `UserContractorSchema`, `UserContractorRequest`
- `apps/api/src/ai/service.ts` — removed `listAIGenerations`
- `apps/api/src/upload/service.ts` — removed `listJobImages`; `@/` imports
- `apps/api/src/app.ts` — updated import `homeJobRouter` (was `jobCollectionRouter`)
- New tests: `job/__tests__/service.test.ts`, `job/__tests__/JobManager.test.ts`
- New tests: `contractor/__tests__/service.test.ts`, `contractor/__tests__/ContractorManager.test.ts`
- New tests: `quote/__tests__/service.test.ts`, `quote/__tests__/QuoteManager.test.ts`
- New tests: `communication/__tests__/route.test.ts`, `communication/__tests__/service.test.ts`, `communication/__tests__/CommunicationManager.test.ts`
- New tests: `integration/__tests__/route.test.ts`, `integration/__tests__/service.test.ts`, `integration/__tests__/IntegrationManager.test.ts`
- New tests: `ai/__tests__/AIGenerationManager.test.ts`, `upload/__tests__/service.test.ts`
- `apps/web/src/lib/api.ts` — added `buildUrl(path, params)` helper
- Updated `job/__tests__/route.test.ts` — removed category-suggestions test blocks

**Key decisions:**
- `suggestTradeCategories` (keyword matching) was removed entirely per THM-20 notes — "hardcoded keyword matching is wrong approach"; the AI diagnostic `suggest_categories` tool call populates categories instead
- Service `getX` / `listX` functions were moved to route handlers calling managers directly via `Promise.all`; services now only contain `createX`, `updateX`, `deleteX`
- `PermissionService.list(manager, userId, role, ...args)` pattern used throughout — no filterUser predicate or callback passed from routes
